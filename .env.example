# Copy to .env and fill in for LIVE_LLM mode.
# If ANTHROPIC_API_KEY is unset, the pipeline automatically runs in MOCK mode
# so tests, CI, and evaluation are fully reproducible without network access.
ANTHROPIC_API_KEY=
LLM_MODE=mock
GROUNDEDNESS_THRESHOLD=0.55
MAX_AGENT_RETRIES=2
